# PresenterFlow - Unity组件绑定框架

## 简介

PresenterFlow是一个专为Unity设计的快速实现组件绑定的轻量级框架。
通过可视化的编辑器工具，傻瓜式勾选点击操作即可轻松完成组件的绑定。大大提高开发效率。
基于MVP (Model-View-Presenter)设计理念，解耦逻辑层与视图层，提高代码的可维护性。

### ✨ 核心特性

- 🎯 **Prefab 原生工作流** -  深度优化的预制体专用方案，完美支持嵌套 Prefab 与复杂层级结构
- 🚀 **自动化代码生成** - 根据用户需要自动生成强类型的 View 类
- 🏗️ **MVP 架构落地** - 严格的Presenter-View 分离，业务逻辑与视图渲染彻底解耦
- 🔗 **高性能索引绑定** - 基于索引路径的组件查找，区别于传统的GameObject.Find
- 🛠️ **完整的编辑器集成** - 在Hierarchy 与 Inspector 中提供所见即所得的可视化操作
- 📦 **外挂式设计** -  被绑定物体无需挂载任何组件或配置，不污染现有组件生态

---

### 📦 安装

#### 方式一：Unity Package (推荐)
1. 下载最新 .unitypackage 从[Releases](https://github.com/kukuni8/PresenterFlow/releases)页面
2. 导入 Unity 项目： Assets > Import Package > Custom Package...

### 初始化配置
1. 创建设置文件：菜单栏 Tools > PresenterFlow > Create Settings
2. 在生成的PFSetting.asset  中配置 View 输出路径（如  Assets/Scripts/Generated/Views ）

#### 基础教程

1. 任意场景中右键/3D Object/Cube ,将Cube拖入Asset目录，制作一个预制体
2. 双击预制体，进入预制体编辑页面
3. 预制体Hierarchy面板中点击AddMetedata按钮
4. 点击 Hierarchy 右侧的三角形图标，勾选需要绑定的组件：  
    左边复选框：多选，字段名为  物体名_组件类型   
    右边单选框：唯一选中，字段名等于物体名
5. 确认 Inspector 中 Bind Components 面板显示字段名，若不满意规则生成的字段名，勾上字段名右侧的自定义勾选框，即可输入自定义字段名。
6. 点击 Hierarchy 中的 Generate 按钮，框架自动创建  Assets/Scripts/Generated/Views/CubeView.cs
7. 创建 CubePresenter.cs 文件，内容如下：
~~~csharp
    [ViewType(typeof(CubeView))]  //一定要标记这个特性
    public partial class CubePresenter : BasePresenter //必须是partial类，必须继承自BasePresenter
    {
        private void Awake()
        {
            Debug.Log($"{View.Cube_BoxCollider.name}");// 在这里就可以访问刚才绑定的组件
        }
    }
~~~
8. 给Cube挂载上CubePresenter.cs 即可
9. <font color=red>若物体改名，层级修改等，重新点击Generate生成即可</font>





